services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: interior_marketplace
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - interior_marketplace_pg:/var/lib/postgresql/data
volumes:
  interior_marketplace_pg:
